# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info
__marimo__

# Configs

*.cfg
.claude
*.log

# Editor/tool backup files (vim/emacs-style) — one of these (.env~) leaked
# live credentials into a commit once already; never again.
*~

# Airflow's own log output, not source
airflow/logs/

# Virtual environments
.venv
.env

# Terraform
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.*
terraform/terraform.tfvars
terraform/*.auto.tfvars
.terraform**

# Downloaded by terraform/cloud-sql-proxy.sh
terraform/.bin/
